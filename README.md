# ScanNet
Developed a multi-threaded, configurable port scanning framework supporting IP, domain, and CIDR inputs with TCP/UDP scanning capabilities. Implemented target validation, resolution, and JSON-based output for automated security analysis.
